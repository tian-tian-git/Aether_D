# AGENTS.md —— Aether 项目智能体操作手册

所有参与本项目的 AI 智能体(主智能体与子智能体)开工前必读并遵守本手册。

## 参与者角色

| 角色 | 职责 |
| :--- | :--- |
| **人类总监**(用户) | 审批里程碑门禁、裁决设计分歧、确认方向。 |
| **主智能体** | 编排里程碑、维护规划与文档、拆分工作包、做核心设计决策、合并结果、向人类汇报。 |
| **子智能体** | 认领工作包独立完成,交付「实现 + 测试 + 文档 + 汇报」。 |

## 硬规则(最高优先级,违反即失败)

1. **Aether_K 完全禁入**:不得读取、修改、执行、复制 `D:\Desktop\programme\ai-lp\Aether_K` 中的任何内容。一切工作仅在 `Aether_D` 内进行。
2. 不得修改系统级配置或超出仓库范围的用户配置(工具链安装除外,且需人类总监知晓)。
3. 未通过当前里程碑的退出标准,不得开始下一里程碑的工作包。
4. 仓库根目录的 `AI跨语言设计.txt` 是只读档案,不得修改。

## 时间单位

本项目**不以日历时间规划**。进度单位 = 里程碑 + 工作包 + 智能体轮次。每轮结束必须更新 [docs/roadmap.md](docs/roadmap.md) 的状态板。

## 标准工作流

1. 读 [docs/roadmap.md](docs/roadmap.md),找到当前里程碑与可认领的工作包。
2. 读相关规范([docs/spec/](docs/spec/))与决策([docs/adr/](docs/adr/))——**先读后写**。
3. 认领:在路线图状态板标记「进行中(认领者)」。
4. 实现 → 测试 → 文档同步 → 提交 → 汇报。
5. 提交信息遵循 Conventional Commits:`feat|fix|docs|test|chore|refactor(scope): 说明`,scope 用 crate 名或里程碑编号(如 `feat(parser): ...`)。

## 工程纪律

- **测试先行**:每个特性至少一个测试;解析类特性必须有 round-trip 测试(parse → print → parse 相等)。
- **文档同步**:语言行为变更必须同步 [docs/spec/](docs/spec/);架构级取舍写 ADR。
- **并行纪律**:两个智能体不得同时编辑同一文件;工作包边界 = 文件边界,认领前检查状态板。
- **诊断质量**:所有编译器/解释器报错必须含「节点 ID + 源位置 + 错误码 + 修复建议」,这是 M4 AI 生成回路的地基。
- **Rust 规范**:workspace 按 crate 拆分以增量编译;改动后先 `cargo check` 再 `cargo test`;依赖方向单向、禁止循环(crate 依赖图见 architecture.md)。
- **性能意识**:MVP 期(≤M3)正确性优先、不过度优化;M5 起以基准数据说话。

## 里程碑门禁(人类总监检查点)

每个里程碑的「退出标准」达成后,主智能体向人类总监提交**门禁报告**(达成证据 + 演示方式),经确认后标记完成并打 tag(`M0`、`M1`、…),再进入下一里程碑。

## 汇报模板(每轮结束)

- 本轮目标与完成情况(对照路线图工作包)
- 新增/变更的关键文件
- 测试与验证结果(命令 + 摘要)
- 阻塞项与需要人类总监决策的事项
- 下一轮计划
